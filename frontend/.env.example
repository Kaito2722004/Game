# Base URL of the FastAPI backend, including the /api/v1 prefix.
VITE_API_BASE_URL=http://localhost:8000/api/v1

# Set to "true" to run against the built-in mock adapter instead of the real
# backend. A visible "Demo Mode" banner is shown whenever this is on.
VITE_USE_MOCK_API=false
